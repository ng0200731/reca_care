"use client";

export default function FontView() {
  return (
    <div className="max-w-3xl mx-auto p-6 lg:p-10">
      <div className="bg-white border border-[var(--border)] rounded-xl p-6">
        <h2 className="text-xl font-semibold text-[var(--foreground)] mb-2">Fonts</h2>
        <p className="text-sm text-[var(--foreground)]/50">View and delete uploaded fonts (coming next).</p>
      </div>
    </div>
  );
}
