"use client";

export default function TranslationView() {
  return (
    <div className="max-w-3xl mx-auto p-6 lg:p-10">
      <div className="bg-white border border-[var(--border)] rounded-xl p-6">
        <h2 className="text-xl font-semibold text-[var(--foreground)] mb-2">Translation Tables</h2>
        <p className="text-sm text-[var(--foreground)]/50">View, edit and delete translation tables (coming next).</p>
      </div>
    </div>
  );
}
